# Plan: Tesla MCV (Россия) — Gates 1–3

Идея (средняя сложность): клон советского MCV для фракции Россия —
`Tesla MCV`: Deploy запрещён, вооружён молниями Tesla tank / Tesla
trooper, прочность x2.

Формат: ровно ТРИ Gates проекта (Implementation / Verification / Close,
см. AGENTS.md). Всё ниже — подзадачи внутри Gates, не новые Gates.
Milestones не изобретаются.

## Gate 1 — Implementation (STARTED 2026-09-21)

Механизм: новый файл-опция `INI\Game Options\Tesla MCV.ini`
(сниппет в формате существующих опций — мёржится поверх правил при
старте матча, распаковка `ra2md.mix` не требуется).

- [x] 1.1 (DONE 2026-09-21). Эталоны сняты: [SMCV] целиком + хвост
      [VehicleTypes] (0..84, CIVP последний → свободный индекс **85**) —
      зеркало rulesmd.ini 1.001 (hzhangxyz/rulesmd.ini:7835/1069,
      провенанс зафиксирован); TTNK/SHK/Locomotor/голоса — из SFJ.ini
      репозитория. Важная поправка: SMCV Strength=**1000** (не 600) →
      цель **2000**; оружие танка TeslaTankWeapon(+E), пехоты ElectricBolt.
      XCC Mixer локально НЕ найден; автоскачивание упёрлось в SourceForge
      (HTML вместо zip, прямые зеркала 404) — каталог
      D:\Soft\Dev-Reverse\xcc-mixer создан, скачать вручную:
      sourceforge.net/projects/xccu (XCC Mixer 1.44). Для Gate 1.2 не
      блокер: значения закрыты зеркалом, финал — Gate 2 в игре.
- [x] 1.2 (DONE-черновик, 2026-09-21). `INI/Game Options/Tesla MCV.ini`
      финализирован по эталонам: 85=TESLMCV, клон SMCV дословно кроме:
      нет DeploysInto/DeploySound, TeslaTankWeapon(+E), Strength=2000,
      Owner=Russians, TechLevel=-1, VoiceAttack от теслы. Выдуманные
      значения убраны (Armor/speed/sight/size/zone — теперь клон).
- [x] Lua-драйвер (2026-09-21, сверх плана): `scripts/mods/tesla_mcv/`
      (main/mod.json/HOW_TO_USE) — T спавнит TESLMCV и печатает
      PASS/FAIL (type/hp-x2/kind/moveto). Харнес
      `tools/tmp/tesla_mcv_test.lua`: 10/10. В active_mods НЕ добавлен
      (включать только на тесты); в Changelog НЕ входит (не верифицирован).
      (Детали дизайна 1.2 — в самом `Tesla MCV.ini`, дубли не ведутся.)
- [ ] 1.3. Статическая проверка: все ключи существуют в YR 1.001,
      индекс `[VehicleTypes]` уникален, нет опечаток в ID оружия/типов,
      файл в кодировке без BOM-проблем.

Stretch (только если база зашла): стройвариант —
`TechLevel`, `Prerequisite=NAHAND,NAREFN` (аналог War Factory),
`Cost`, `Cameo` (реюз иконки SMCV/TTNK), `BuildLimit=1`.

## Gate 2 — Verification

- [ ] 2.1. Загрузка: новый матч CnCNet с включённой опцией — старт без
      краша, `TESLMCV` в реестре типов (косвенно: спавн успешен).
- [ ] 2.2. Функционал через LuaAPI (`house:SpawnUnit("TESLMCV",...)`):
      - едет (`MoveTo` выполняется);
      - стреляет молниями (визуал дуги + урон цели; сравнить с TTNK);
      - `GetMaxHealth()` = 2x от SMCV, измерено, а не «по коду»;
      - Deploy недоступен (нет курсора/действия развернуться).
- [ ] 2.3. Soak: стычка с ИИ 5+ минут с заспавненным Tesla MCV —
      без вылетов/расхождений; ИИ его игнорирует как should (не обязан
      использовать).
- [ ] 2.4. (Если взят stretch) Постройка с завода: цена/иконка/лимит,
      баланс на усмотрение автора.

Критерий Gate 2: все 2.1–2.3 PASS по свежему логу/наблюдению.
INCONCLUSIVE там, где нет прямых улик, — честно фиксируем.

## Gate 3 — Close

- [ ] HOW_TO_USE рядом с опцией (включение в лобби, что делает юнит).
- [ ] Запись в `Changelog.txt` (мод рабочий: версия 1.0.0 + доказательства).
- [ ] Известные ограничения в доке: имя (CSF/литерал), ИИ не строит и не
      водит, безбашенная стрельба ведётся корпусом, баланс стройварианта
      не финальный.
- [ ] Финальный прогон существующих харнесов (регрессий нет — INI-опция
      Lua-код не трогает, но ворота есть ворота).

## Риски (приняты заранее)

- Оружие без башни может стрелять «из корпуса» — визуально приемлемо
  для MVP, правится `PrimaryFireFLH`/артом позже.
- Без `DeploysInto` deploy-курсор должен пропасть; если движок
  захардкодил deploy для MCV-типа — fallback: `DeployTime` огромный /
  отдельный разбор (станет отдельной задачей, не раздуваем эту).
- CSF-имя: без Ares-литерала может показать ключ вместо «Tesla MCV» —
  косметика, на Gates не влияет.
